#include <stdio.h>  //Prerocessor statement for basic input output added

int main() {    // Main function execution starts here.

    int a = 34, b = 89, c = 98, d = 65;  // Initialising varibled for storing values
    float z = a % b + c / d;  // Calculating the result and storing in the variable z of data type float
    printf("Result of Z is:- %f", z);  // Printing results
    return 0;   //Returning integer value after succesfull execution
}   //program ends here